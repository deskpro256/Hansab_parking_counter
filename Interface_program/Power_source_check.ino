//============================[POWER_SOURCE]==========================================================
void powerSource() {
// check if external power is connected
// if it is, continue
// else, check battery voltage and
// if connected to PC, send out that you are on battery
}
